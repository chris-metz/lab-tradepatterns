export const metadata = {
  title: "Trade Patterns",
  description: "Crypto trading pattern analysis",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="de">
      <body>{children}</body>
    </html>
  );
}
